# Content Moderation Service Configuration
# Model ID = Inference first, then fallback to Space. URL = Space only.

HF_API_URL=Falconsai/nsfw_image_detection

# Optional fallback when Inference fails (default: phantomfury-kaleidoscope-content-moderation)
# HF_MODERATION_SPACE_URL=https://phantomfury-kaleidoscope-content-moderation.hf.space/classify

# Get your token from: https://huggingface.co/settings/tokens (needs Inference API scope)
HF_API_TOKEN=your_huggingface_api_token_here

# Service Configuration
LOG_LEVEL=INFO
