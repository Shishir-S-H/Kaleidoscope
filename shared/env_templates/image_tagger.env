# Image Tagger Service Configuration
# Model ID = Inference first, then fallback to Space. URL = Space only.

HF_API_URL=openai/clip-vit-large-patch14

# Optional fallback when Inference fails (default: phantomfury-kaleidoscope-image-tagger)
# HF_TAGGER_SPACE_URL=https://phantomfury-kaleidoscope-image-tagger.hf.space/tag

# Get your token from: https://huggingface.co/settings/tokens (needs Inference API scope)
HF_API_TOKEN=your_huggingface_api_token_here

# Tagging Configuration
DEFAULT_TOP_N=5
DEFAULT_THRESHOLD=0.05

# Service Configuration
LOG_LEVEL=INFO
