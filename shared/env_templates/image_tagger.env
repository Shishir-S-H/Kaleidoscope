# Image Tagger Service Configuration - API Mode (Lightweight)
# Uses remote Hugging Face API instead of local models

# Hugging Face API Configuration
# URL to your deployed HF Space for image tagging
HF_API_URL=https://phantomfury-kaleidoscope-image-tagger.hf.space/tag

# Hugging Face API Token (optional for public models, required for private/gated models)
# Get your token from: https://huggingface.co/settings/tokens
HF_API_TOKEN=your_huggingface_api_token_here

# Tagging Configuration
# Number of top tags to return
DEFAULT_TOP_N=5

# Minimum confidence threshold for tags (0.0 to 1.0)
DEFAULT_THRESHOLD=0.05

# Service Configuration
LOG_LEVEL=INFO
