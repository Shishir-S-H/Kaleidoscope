# Scene Recognition Service Configuration
# Uses image-classification via Inference API (model generates its own labels).
# No hardcoded scene pool -- the model discovers what's in the image.

HF_API_URL=google/vit-base-patch16-224

# Optional fallback when Inference fails (default: phantomfury-kaleidoscope-scene-recognition)
# HF_SCENE_SPACE_URL=https://phantomfury-kaleidoscope-scene-recognition.hf.space/recognize

# Get your token from: https://huggingface.co/settings/tokens (needs Inference API scope)
HF_API_TOKEN=your_huggingface_api_token_here

# Scene Configuration
DEFAULT_THRESHOLD=0.05

# Service Configuration
LOG_LEVEL=INFO
